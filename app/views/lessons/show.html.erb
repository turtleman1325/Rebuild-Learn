<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @lesson.name %>
</p>

<p>
  <strong>Content:</strong>
  <%= @lesson.content %>
</p>

<%= link_to 'Edit', edit_lesson_path(@lesson) %> |
<%= link_to 'Back', lessons_path %>
<%= link_to('Previous Lesson', @lesson.previous) if @lesson.previous %>
<%= link_to('Next Lesson', @lesson.next) if @lesson.next %>
